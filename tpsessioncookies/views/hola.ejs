<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= title %></title>
        <link rel='stylesheet' href='/stylesheets/style.css'/>
        <!-- link boostrap -->
        <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
      </head>
<body class="<%= locals.session && session.usuario ? `bg-${session.usuario.color}` : locals.session && session.color ? `bg-${session.color}` : ""  %> ">
    <div class="container pt-5 w-md-50">
        <h1>Hola <%= session.usuario.name %>, Bienvenida  </h1>
        <h4>Elegiste el color: <%= session.usuario.color %> </h4>
        <h4>Tu email es: <%= session.usuario.email %>  </h4>
        <h4>Tu edad es: <%= session.usuario.edad %>  </h4>
        <% if (locals.session && session.usuario) { %>
            <a href="/cerrar-session" class="btn btn-warning">Cerrar Sesión</a></button>
         
        <% } %>
    </div>
</body>
</html>